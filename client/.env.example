# ==================== API CONFIGURATION ====================
# Backend API URL
VITE_API_URL=http://localhost:8080/api/v1

# ==================== RAZORPAY CONFIGURATION ====================
# Razorpay Key ID (Public Key)
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id

# ==================== CCAVENUE CONFIGURATION ====================
# Enable/Disable CCAvenue payment option
VITE_CCAVENUE_ENABLED=true

# ==================== NOTES ====================
# 1. Frontend Environment Variables:
#    - All Vite environment variables must start with VITE_
#    - These variables are exposed to the client-side code
#    - Never store sensitive keys (like secrets) in frontend .env
#
# 2. Payment Gateway Keys:
#    - Only public/publishable keys should be in frontend .env
#    - Secret keys must ONLY be in backend .env
#    - Razorpay Key ID is safe to expose (it's a public key)
#    - CCAvenue encryption happens on backend only
#
# 3. Development vs Production:
#    - Use test keys for development
#    - Use production keys for live environment
#    - Update VITE_API_URL to production URL when deploying
#
# 4. Security:
#    - Never commit .env file to version control
#    - Use .env.local for local overrides
#    - Validate all payment responses on backend
